meta {
  name: Insert
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/category-global/insert
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Periféricos"
  }
}
